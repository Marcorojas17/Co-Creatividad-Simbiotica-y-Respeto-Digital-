apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cymatic-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cymatic-web
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
